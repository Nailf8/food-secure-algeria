from setuptools import setup, find_packages

setup(
    name="sid",
    version="0.1.0",
    author="Naïl Ferroukhi",
    author_email="nferroukhi@ecovadis.com",
    packages=find_packages(),  # Automatically find all packages (directories with __init__.py)
)
